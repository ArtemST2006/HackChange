# Академия ПСБ - Frontend

Образовательная платформа "Академия ПСБ" - фронтенд приложение на React + TypeScript.

## 🚀 Технологии

- **React 18** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **React Router** - маршрутизация
- **CSS Variables** - для стилизации (без внешних библиотек)

## 📁 Структура проекта

```
src/
├── assets/
│   └── styles/          # Глобальные стили
│       ├── global.css   # Базовые стили
│       └── variables.css# CSS переменные
├── components/
│   └── shared/          # Переиспользуемые компоненты
│       ├── Button/      # Компонент кнопки
│       ├── Input/       # Компонент input
│       ├── Card/        # Компонент карточки
│       ├── Slider/      # Компонент слайдера
│       └── Header/      # Хедер с меню
├── context/
│   └── AuthContext.tsx  # Контекст авторизации
├── hooks/               # Custom React hooks
│   ├── useApi.ts        # Хук для API запросов
│   ├── useDashboard.ts  # Хук для данных дашборда
│   ├── useCourses.ts    # Хук для курсов
│   └── useHomework.ts   # Хук для домашних заданий
├── pages/
│   ├── Login/           # Страница входа
│   ├── Register/        # Страница регистрации
│   ├── Dashboard/       # Главная страница
│   ├── Courses/         # Страница курсов
│   └── Homework/        # Страница домашних заданий
├── services/
│   └── api/             # API сервисы
│       ├── client.ts           # HTTP клиент
│       ├── auth.service.ts     # Авторизация API
│       ├── dashboard.service.ts# Дашборд API
│       ├── courses.service.ts  # Курсы API
│       └── homework.service.ts # Домашние задания API
├── types/
│   └── index.ts         # TypeScript типы
├── utils/
│   └── mockData.ts      # Моковые данные
├── App.tsx              # Главный компонент с роутингом
└── main.tsx             # Точка входа
```

## 🎨 Дизайн

Дизайн основан на спецификации из `DETAILED_DESIGN_SPECIFICATION.md`:

- **Цвета**: Темно-синий (#2E1A7C), Оранжевый (#FF6B5B)
- **Шрифт**: Inter
- **Стиль**: Современный, минималистичный с градиентами
- **Адаптивность**: Мобильная и десктопная версии

## 🛠 Установка и запуск

### 1. Установка зависимостей

```bash
cd frontend
npm install
```

### 2. Запуск dev-сервера

```bash
npm run dev
```

Приложение откроется по адресу: http://localhost:5173

### 3. Сборка для продакшена

```bash
npm run build
```

### 4. Превью продакшн-сборки

```bash
npm run preview
```

## 📝 Доступные страницы

### Реализованные:
- ✅ `/login` - Страница авторизации
- ✅ `/register` - Страница регистрации
- ✅ `/dashboard` - Главная страница (дашборд)

### Функциональность:
- ✅ Авторизация (mock)
- ✅ Регистрация (mock)
- ✅ Protected routes (защищенные маршруты)
- ✅ Header с burger menu
- ✅ Карточки курсов
- ✅ Статистика студента
- ✅ Ближайшие дедлайны
- ✅ Адаптивный дизайн

## 🔐 Тестовые данные

Для входа можно использовать любые данные (mock авторизация):

```
Email: любой@email.com
Пароль: любой пароль
```

## 🎯 Основные компоненты

### Button
```tsx
import { Button } from './components/shared';

<Button variant="primary" size="md" fullWidth>
  Кнопка
</Button>
```

Варианты: `primary`, `secondary`, `outline`, `ghost`, `danger`
Размеры: `sm`, `md`, `lg`

### Input
```tsx
import { Input } from './components/shared';

<Input
  label="Email"
  type="email"
  placeholder="example@email.com"
  fullWidth
/>
```

### Card
```tsx
import { Card } from './components/shared';

<Card hoverable onClick={() => {}}>
  <div>Контент карточки</div>
</Card>
```

## 📱 Адаптивность

- **Desktop**: > 1200px - полная версия
- **Tablet**: 768px - 1200px - адаптированная версия
- **Mobile**: < 768px - мобильная версия с burger menu

## 🔌 API Интеграция

Проект готов к интеграции с бэкендом:

### Настройка подключения

1. Создайте файл `.env` в корне `frontend/`:
```bash
cp .env.example .env
```

2. Укажите URL вашего API:
```
VITE_API_BASE_URL=http://localhost:3000/api
```

### Использование API сервисов

```tsx
// Прямое использование сервисов
import { dashboardService, coursesService } from './services/api';

const data = await dashboardService.getDashboardData();
const courses = await coursesService.getAllCourses();
```

### Использование хуков

```tsx
// Автоматическая загрузка данных с loading/error состояниями
import { useDashboard, useCourses } from './hooks';

function MyComponent() {
  const { data, loading, error, refetch } = useDashboard();

  if (loading) return <div>Loading...</div>;
  if (error) return <div>Error: {error.message}</div>;

  return <div>{/* Use data */}</div>;
}
```

### Документация для бэкенда

Полная документация по API эндпоинтам и форматам данных: **`API_INTEGRATION.md`**

## 🔧 Расширение функционала

Проект подготовлен для дальнейшего расширения:

1. Добавьте новые страницы в `src/pages/`
2. Создайте компоненты в `src/components/`
3. Обновите типы в `src/types/index.ts`
4. Добавьте маршруты в `src/App.tsx`
5. Создайте новые API сервисы в `src/services/api/`
6. Добавьте custom хуки в `src/hooks/`

## 📄 Лицензия

Проект создан для хакатона ПСБ Hack&Change 2025.

## 👥 Команда

Фронтенд разработан на React + TypeScript согласно требованиям спецификации.
