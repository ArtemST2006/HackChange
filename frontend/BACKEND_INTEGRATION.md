# Руководство по интеграции фронтенда с бэкендом

## 📋 Общая информация

Фронтенд полностью подготовлен для работы с бэкендом. Реализован гибкий механизм переключения между моковыми данными (для разработки) и реальным API.

## 🚀 Быстрый старт

### 1. Настройка окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

### 2. Конфигурация API

Отредактируйте `.env`:

```env
# URL вашего бэкенда
<<<<<<< HEAD
VITE_API_BASE_URL=http://localhost:8000
=======
VITE_API_BASE_URL=http://localhost:3000/api
>>>>>>> origin/Front_bombas

# Использовать моковые данные (true/false)
VITE_USE_MOCK_DATA=false  # false для работы с реальным API
```

### 3. Запуск

```bash
npm install
npm run dev
```

## 📚 Структура проекта

```
frontend/
├── src/
│   ├── api/              # API клиенты и сервисы
│   │   ├── client.ts     # Базовый HTTP клиент
│   │   ├── auth.service.ts
│   │   ├── courses.service.ts
│   │   ├── dashboard.service.ts
│   │   └── homework.service.ts
│   ├── components/
│   │   ├── Quiz/         # Компонент интерактивных тестов
│   │   └── ...
│   ├── context/
│   │   └── AuthContext.tsx  # Контекст аутентификации
│   ├── data/
│   │   └── pythonCourse.ts  # Данные курса Python с тестами
│   ├── hooks/            # Custom React hooks для API
│   ├── types/            # TypeScript типы
│   └── utils/
│       └── mockData.ts   # Моковые данные для разработки
├── .env                  # Конфигурация (не в git)
└── .env.example          # Пример конфигурации
```

## 🔧 Режимы работы

### Режим разработки (Mock Data)

Для работы без бэкенда установите в `.env`:

```env
VITE_USE_MOCK_DATA=true
```

В этом режиме:
- Используются моковые данные из `utils/mockData.ts`
- Не требуется запущенный бэкенд
- Симулируются задержки API
- Данные сохраняются только в localStorage

### Режим продакшн (Real API)

Для работы с реальным бэкендом:

```env
VITE_USE_MOCK_DATA=false
VITE_API_BASE_URL=http://your-backend-url/api
```

В этом режиме:
- Все запросы идут на реальный API
- Используется JWT аутентификация
- Данные сохраняются на сервере

## 🔐 Аутентификация

### Как работает

1. При логине/регистрации бэкенд возвращает JWT token
2. Token сохраняется в `localStorage` под ключом `authToken`
3. Все последующие запросы автоматически включают header:
   ```
   Authorization: Bearer <token>
   ```
4. При старте приложения проверяется валидность токена через `/auth/me`

### Реализация в коде

```typescript
// AuthContext автоматически определяет режим работы
const USE_MOCK_DATA = import.meta.env.VITE_USE_MOCK_DATA === 'true';

// В зависимости от режима используется mock или real API
if (USE_MOCK_DATA) {
  // Mock login
} else {
  // Real API login через authService
  const response = await authService.login(credentials);
}
```

## 📡 API Endpoints

Все endpoint'ы описаны в файле `API_INTEGRATION.md`. Основные:

### Аутентификация
- `POST /auth/login` - Вход
- `POST /auth/register` - Регистрация
- `POST /auth/logout` - Выход
- `GET /auth/me` - Получить текущего пользователя
- `POST /auth/refresh` - Обновить токен

### Dashboard
- `GET /dashboard` - Данные дашборда
- `GET /dashboard/statistics` - Статистика пользователя

### Курсы
- `GET /courses` - Все курсы (с фильтрами)
- `GET /courses/my` - Мои курсы
- `GET /courses/:id` - Детали курса
- `GET /courses/:id/modules` - Модули курса
- `POST /courses/:id/enroll` - Записаться на курс

### Домашние задания
- `GET /homeworks/my` - Мои задания
- `GET /homeworks/:id` - Детали задания
- `POST /homeworks/:id/submit` - Отправить решение
- `GET /homeworks/:id/submission` - Моя отправка

### Тесты (новое!)
- `GET /quizzes/:id` - Получить тест
- `POST /quizzes/:id/submit` - Отправить ответы
- `GET /quizzes/:id/attempts` - История попыток

### Упражнения с кодом (новое!)
- `GET /exercises/:id` - Получить упражнение
- `POST /exercises/:id/run` - Запустить код
- `POST /exercises/:id/submit` - Отправить решение

## 🎓 Курс Python

Создан полноценный курс Python с:

### Интерактивные тесты
- 3 теста по модулям (Основы, Циклы, ООП)
- Разные типы вопросов:
  - Одиночный выбор
  - Множественный выбор
  - Правда/Ложь
  - С кодом
- Автоматическая проверка
- Показ правильных ответов
- Объяснения после прохождения

### Практические задания
- Калькулятор (Модуль 2)
- Функции для списков (Модуль 3)
- Класс Автомобиль (Модуль 4)
- Автоматическая проверка через тест-кейсы
- Скрытые тест-кейсы для полной проверки

### Использование в коде

```typescript
import { quiz1_basics, exercise1_calculator } from '@/data/pythonCourse';
import { Quiz } from '@/components/Quiz/Quiz';

// Использование компонента Quiz
<Quiz
  quiz={quiz1_basics}
  onComplete={(attempt) => {
    console.log('Score:', attempt.score);
    console.log('Passed:', attempt.passed);
  }}
/>
```

## 🔄 Примеры использования API

### Использование хуков

```typescript
import { useDashboard, useCourses, useMyHomeworks } from '@/hooks';

function Dashboard() {
  // Автоматически использует mock или real API
  const { data, loading, error } = useDashboard();

  if (loading) return <Loader />;
  if (error) return <Error message={error.message} />;

  return <div>{data.statistics.activeCourses} активных курсов</div>;
}
```

### Прямое использование сервисов

```typescript
import { coursesService, homeworkService } from '@/services/api';

// Получить курсы с фильтрами
const courses = await coursesService.getAllCourses({
  category: 'Программирование',
  difficulty: 'beginner'
});

// Отправить домашнее задание
const formData = new FormData();
formData.append('files', file);
formData.append('textAnswer', 'Мой ответ');

const submission = await homeworkService.submitHomework('hw-id', formData);
```

## 🧪 Тестирование интеграции

### Проверка подключения

1. Убедитесь что бэкенд запущен
2. Установите `VITE_USE_MOCK_DATA=false` в `.env`
3. Откройте DevTools → Network
4. Попробуйте войти/зарегистрироваться
5. Проверьте что запросы идут на ваш API

### Отладка

В случае ошибок:

1. Проверьте CORS на бэкенде:
   ```
   Access-Control-Allow-Origin: http://localhost:5173
   Access-Control-Allow-Credentials: true
   ```

2. Проверьте формат ответов API (должны соответствовать `ApiResponse<T>`)

3. Проверьте консоль браузера на ошибки

## 📝 Требования к бэкенду

### Формат ответов

Все endpoint'ы должны возвращать:

```typescript
{
  "data": T,           // Данные запроса
  "message": string,   // Опциональное сообщение
  "success": boolean   // Статус успеха
}
```

### Формат ошибок

```typescript
{
  "message": string,
  "errors": { field: string[] },  // Опционально
  "statusCode": number
}
```

### CORS настройки

```javascript
app.use(cors({
  origin: 'http://localhost:5173',
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'PATCH', 'DELETE'],
  allowedHeaders: ['Content-Type', 'Authorization']
}));
```

### JWT аутентификация

- Token в header: `Authorization: Bearer <token>`
- Endpoint для проверки: `GET /auth/me`
- Refresh token (опционально): `POST /auth/refresh`

## 🎯 Что уже готово

✅ API клиент с автоматической обработкой ошибок
✅ Аутентификация (JWT + Refresh Token)
✅ Все основные сервисы (auth, courses, homework, dashboard)
✅ Custom hooks для удобной работы с API
✅ Переключение mock/real режимов
✅ TypeScript типы для всех данных
✅ Компонент интерактивных тестов
✅ Реальный контент курса Python
✅ Система проверки ответов на тесты
✅ Файловая загрузка для домашних заданий

## 🚧 Что нужно реализовать на бэкенде

1. **Тесты и квизы**
   - Endpoint для получения теста
   - Проверка ответов студента
   - Сохранение попыток
   - История прохождения

2. **Упражнения с кодом**
   - Выполнение Python кода в изолированной среде (sandbox)
   - Проверка тест-кейсов
   - Возврат результатов выполнения
   - Лимиты на время выполнения

3. **Система оценивания**
   - Автоматическая проверка тестов
   - Ручная проверка домашних заданий
   - Комментарии преподавателя
   - Инлайн-комментарии к коду

## 📞 Поддержка

При возникновении вопросов по интеграции:
1. Проверьте `API_INTEGRATION.md` для деталей по endpoint'ам
2. Изучите типы в `src/types/index.ts`
3. Посмотрите примеры в `src/services/api/*.service.ts`

## 🔗 Полезные ссылки

- [API Integration Guide](./API_INTEGRATION.md) - Детальное описание API
- [TypeScript Types](./src/types/index.ts) - Все типы данных
- [Python Course Data](./src/data/pythonCourse.ts) - Контент курса
