# Быстрый старт

## 🚀 Запуск за 3 шага

### 1. Установка
```bash
cd commit-to-learn-frontend
npm install
```

### 2. Запуск dev-сервера
```bash
npm run dev
```

### 3. Открыть в браузере
```
http://localhost:5173
```

## 🔐 Вход в систему

Используйте любые данные для входа (mock-авторизация):

```
Email: test@example.com
Пароль: password123
```

## 📱 Что реализовано

### ✅ Страницы
- Авторизация (`/login`)
- Регистрация (`/register`)
- Дашборд (`/dashboard`)

### ✅ Компоненты
- Header с burger menu
- Кнопки (Button)
- Поля ввода (Input)
- Карточки (Card)

### ✅ Функции
- Защищенные маршруты (ProtectedRoute)
- Контекст авторизации (AuthContext)
- Mock данные для демонстрации
- Адаптивный дизайн

## 🎨 Дизайн-система

**Цвета:**
- Темно-синий: `#2E1A7C`
- Оранжевый: `#FF6B5B`

**Компоненты:**
- Все компоненты в `src/components/shared/`
- Типы в `src/types/index.ts`
- Mock данные в `src/utils/mockData.ts`

## 📂 Структура

```
src/
├── components/shared/  # Переиспользуемые компоненты
├── context/           # React Context (авторизация)
├── pages/             # Страницы приложения
├── types/             # TypeScript типы
├── utils/             # Утилиты и mock данные
└── assets/styles/     # Глобальные стили
```

## 🔧 Команды

```bash
npm run dev      # Запуск dev-сервера
npm run build    # Сборка для продакшена
npm run preview  # Превью продакшн-сборки
npm run lint     # Проверка кода
```

## 💡 Советы

1. **Навигация:** Используйте burger menu (☰) для перехода между страницами
2. **Данные:** Все данные моковые, реальный API не подключен
3. **Расширение:** Добавляйте новые страницы в `src/pages/`
4. **Компоненты:** Переиспользуйте компоненты из `src/components/shared/`

## 🐛 Проблемы?

Если возникли проблемы:

1. Убедитесь, что Node.js установлен
2. Удалите `node_modules` и запустите `npm install` заново
3. Проверьте порт 5173 - должен быть свободен

## 📖 Подробная документация

См. `README.md` для полной документации.
